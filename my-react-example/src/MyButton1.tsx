// Importing React to use JSX and create the functional component
import React from 'react';

// Defining the MyButton1 component
const MyButton1: React.FC = () => {
  return (
    // Rendering a button element with text
    <button>I'm a button</button>
  );
};

// Exporting the MyButton1 component so it can be used in other parts of the application
export default MyButton1;
